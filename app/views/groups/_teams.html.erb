<ul class="list-group">
  <% @group.teams.each do |team| %>
    <li class="list-group-item"><%= team.name %></li>
    <%#= link_to team.name, team_path(team), class: "btn btn-link" %>
  <% end %>
</ul>
